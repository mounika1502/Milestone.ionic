import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mile.stone',
  appName: 'Milestone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
